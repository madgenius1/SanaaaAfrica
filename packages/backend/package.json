{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env PAYLOAD_CONFIG_PATH=payload.config.ts nodemon",
    "build": "cross-env PAYLOAD_CONFIG_PATH=payload.config.ts payload build",
    "start": "cross-env PAYLOAD_CONFIG_PATH=payload.config.ts node dist/server.js",
    "seed": "cross-env PAYLOAD_CONFIG_PATH=payload.config.ts ts-node scripts/seed.ts",
    "generate:types": "cross-env PAYLOAD_CONFIG_PATH=payload.config.ts payload generate:types"
  },
  "dependencies": {
    "payload": "^2.0.0",
    "express": "^4.18.2",
    "mongodb": "^6.0.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.0.0",
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.0"
  }
}